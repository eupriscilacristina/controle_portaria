rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /acessos/{doc} {
      allow read, write: if request.auth != null;
    }

    match /pessoas/{doc} {
      allow read, write: if request.auth != null;
    }
  }
}
